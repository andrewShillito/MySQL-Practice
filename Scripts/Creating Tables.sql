CREATE TABLE cats (
    name VARCHAR(100),
    breed VARCHAR(100),
    age INT
);

CREATE TABLE people (
	first_name VARCHAR(30),
    last_name VARCHAR(30),
    age TINYINT,
    birth_place VARCHAR(50)
);